---
# role: fail2ban
# file: defaults/main.yml

# Persistent ban database path; use 'None' to disable persistence.
fail2ban_dbfile: /var/lib/fail2ban/fail2ban.sqlite3

# Maximum number of matched log entries stored per ban in the database.
fail2ban_dbmaxmatches: 10

# Retention period for stored bans, in seconds or Fail2ban duration notation.
fail2ban_dbpurgeage: 86400

# Daemon logging level.
fail2ban_loglevel: INFO

# Log output destination, such as a file path, SYSLOG, or STDERR.
fail2ban_logtarget: /var/log/fail2ban.log

# Daemon process ID file path.
fail2ban_pidfile: /var/run/fail2ban/fail2ban.pid

# Unix socket path used by fail2ban-client.
fail2ban_socket: /var/run/fail2ban/fail2ban.sock

# Thread stack size in KiB; zero uses the platform default.
fail2ban_stacksize: 0

# Syslog socket path; auto selects the platform socket when logging to SYSLOG.
fail2ban_syslogsocket: auto

# INI options in jail.local; state defaults to present and absent needs no value.
fail2ban_jail_local:
  - section: DEFAULT
    option: bantime
    value: 3600
  - section: DEFAULT
    option: findtime
    value: 600
  - section: DEFAULT
    option: maxretry
    value: 3
  - section: DEFAULT
    option: ignoreip
    value: "127.0.0.1/8 ::1 {{ ansible_facts.default_ipv4.network }}/{{ ansible_facts.default_ipv4.netmask }}"
  - section: DEFAULT
    option: destemail
    value: root@localhost
  - section: DEFAULT
    option: sender
    value: "root@{{ ansible_facts.fqdn }}"
  - section: DEFAULT
    option: mta
    value: sendmail
  - section: DEFAULT
    option: action
    value: "%(action_)s"

# Named action.d/*.local files and their INI options; absent options need no value.
fail2ban_actions: []
  # - name: firewallcmd-custom
  #   options:
  #     - section: INCLUDES
  #       option: before
  #       value: "firewallcmd-common"
  #     - section: Definition
  #       option: actionstart
  #       value: "..."

# Named filter.d/*.local files and their INI options; absent options need no value.
fail2ban_filters: []
  # - name: vaultwarden
  #   options:
  #     - section: INCLUDES
  #       option: before
  #       value: common.conf
  #     - section: Definition
  #       option: failregex
  #       value: '^.*Username or password is incorrect\. Try again\. IP: <ADDR>\. Username:.*$'
  #     - section: Definition
  #       option: ignoreregex
  #       value: ""

# Named jail.d/*.local files and their INI options; absent options need no value.
fail2ban_jails: []
  # - name: vaultwarden
  #   options:
  #     - section: vaultwarden
  #       option: enabled
  #       value: true
  #     - section: vaultwarden
  #       option: port
  #       value: "80,443,8081"
  #     - section: vaultwarden
  #       option: filter
  #       value: vaultwarden
  #     - section: vaultwarden
  #       option: logpath
  #       value: "/var/log/vaultwarden/vaultwarden.log"
  #     - section: vaultwarden
  #       option: maxretry
  #       value: 5
  #     - section: vaultwarden
  #       option: bantime
  #       value: 3600
  #     - section: vaultwarden
  #       option: findtime
  #       value: 600
  #     - section: vaultwarden
  #       option: banaction
  #       value: "%(banaction_allports)s"
